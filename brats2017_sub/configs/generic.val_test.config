ids = meta.extractor
    property = "patient_ids"
    module = dataset

dataset = dataset_wrapper.cached
    dataset = dataset_wrapper.normalized_sub
        dataset = dataset.brats2017(data_path = data_path)

